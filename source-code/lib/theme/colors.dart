import 'package:flutter/material.dart';

class C {
  // Backgrounds
  static const bg = Color(0xFF1A1A2E); // dark navy (header)
  static const surface = Color(0xFFF4F4F7); // light grey (body)
  static const navBg = Color(0xFF1E1B3A); // bottom nav

  // Base
  static const white = Colors.white;
  static const badge = Colors.red;

  // Purple
  static const purple = Color(0xFF4A3F8F);
  static const purpleLight = Color(0xFFEDEBF8);

  // Gold / Yellow
  static const yellow = Color(0xFFFFD84D); // bright yellow (logo)
  static const gold = Color(0xFFFFB300); // gold (links, source)
  static const goldDark = Color(0xFFD4A800); // gold dark (filter reset)

  // Text
  static const textDark = Color(0xFF14142B);
  static const textGrey = Color(0xFF8E8E9A);

  // Green
  static const green = Color(0xFF4CAF50);
  static const greenLight = Color(0xFFE8F5E9);

  // Border
  static const border = Color(0xFFE0DED8);
}
